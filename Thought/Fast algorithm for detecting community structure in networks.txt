已有算法的缺点：
基于边的betweenness性质的删除算法         复杂度 O(m2*n) or O(n3)  复杂度太高

根据Finding and evaluating ...的论文中Q的建立，其实最优化Q是最直接的方法，通过在所有的分割中找到最大的Q值应该是最直接的方法，但是这个方法计算量太大，
所以之前采用分裂的基于betweenness的算法。

穷举所有的分割n点到g个社团会是一个stirling number of second kind (？？这是什么？？)  且其可能值是非闭式（？？）  其值至少是指数级增长
近似最优化方法可能可以解决
如：模拟退火
遗传算法
Q的增量是怎么计算出来的？？  page2 para3

分级聚类方法？？找论文
